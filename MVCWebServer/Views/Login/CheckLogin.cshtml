
@{
    ViewData["Title"] = "Checking Login";
}
@model MVCWebServer.Models.User

@{
    if (Model.auth)
    {
        @Html.Label(Model.username + " welcome back!");
    }
    else
    {
            @Html.Label("Not a real user!");
        }
    }

